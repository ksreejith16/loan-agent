# =====================================================
# N CARE HOSPITAL — Frontend environment variables
# Copy this file to `.env` and fill in real values.
# =====================================================

# Base URL of the deployed backend API.
# Leave UNSET to run the frontend in DEMO MODE
# (OTPs are generated locally and shown in a toast / console).
# Example: VITE_API_BASE=https://api.ncarehospital.com
VITE_API_BASE=
